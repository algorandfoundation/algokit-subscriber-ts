{
  "name": "algorand-indexer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --watch .env -r dotenv/config ./index.ts",
    "watch": "cross-env RUN_LOOP=true ts-node-dev --transpile-only --watch .env -r dotenv/config ./index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "lint": "eslint \"src/**/*.ts\"",
    "lint:fix": "eslint \"src/**/*.ts\" --fix"
  },
  "author": "MakerX",
  "devDependencies": {
    "@makerx/eslint-config": "3.1.0",
    "@makerx/prettier-config": "2.0.0",
    "@makerx/ts-config": "1.0.1",
    "cross-env": "^7.0.3",
    "eslint": "8.22.0",
    "prettier": "3.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.0"
  },
  "dependencies": {
    "@algorandfoundation/algokit-utils": "^4.1.0",
    "algosdk": "^2.7.0",
    "dotenv": "^16.3.1"
  }
}
